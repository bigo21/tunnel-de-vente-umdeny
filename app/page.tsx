import { Tunnel } from "@/components/tunnel/tunnel";

/**
 * Destination unique des liens en bio et des publicités : tout le parcours
 * tient sur cette page, qui se déploie écran par écran.
 */
export default function Page() {
  return <Tunnel />;
}
